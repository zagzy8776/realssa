<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Trust system CAs only for production — dropping user CAs prevents MITM proxies -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Explicitly trust the RealSSA API domain -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">realssanews.com.ng</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Allow Capacitor's own localhost bridge (required for WebView assets) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
